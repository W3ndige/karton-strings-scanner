# karton-strings-scanner
Aurora karton for scanning strings for known heuristics matches.

**Consumes:**
```
{
    "type":     "feature",
    "stage":    "raw"
    "kind":     "strings"
    ...
} 
```
